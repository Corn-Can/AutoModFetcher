modLoader = "javafml"
loaderVersion = "[4,)"
license = "ARR"
issueTrackerURL = "https://github.com/Corn-Can/AutoModFetcher/issues"

[[mods]]
modId = "${id}"
version = "${version}"
displayName = "${name}"
authors = "CornCan"
logoFile = "assets/automodfetcher/icon.png"
description = '''
Automatically syncs the mods a server requires. On join, the server sends its mod manifest;
the client downloads whatever is missing and prompts for a restart.
'''

[[mixins]]
config = "automodfetcher.mixins.json"

[[dependencies.${id}]]
modId = "neoforge"
type = "required"
versionRange = "[21.1,)"
ordering = "NONE"
side = "BOTH"

[[dependencies.${id}]]
modId = "minecraft"
type = "required"
versionRange = "${minecraft}"
ordering = "NONE"
side = "BOTH"
