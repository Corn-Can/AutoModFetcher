# 路線圖

**北極星目標：玩家一鍵進入伺服器。**

*目前支援：Fabric 1.20.1 · Fabric 1.21.1 · NeoForge 1.21.1 · Forge 1.20.1*

底下每一項都用同一個標準衡量——它減少了玩家幾個動作？沒有減少的，排在後面。

---

## 起點：玩家從零到進遊戲要做幾件事

*（這是路線圖寫下時的狀態，用來衡量後續每一項的價值。）*

| # | 動作 | 類型 |
|---|---|---|
| 1 | 用 Fabric 安裝程式裝 Loader 1.20.1 | 手動安裝 |
| 2 | 下載 Fabric API，丟進 `mods/` | 手動安裝 |
| 3 | 下載 AutoModFetcher，丟進 `mods/` | 手動安裝 |
| 4 | 開遊戲、加入伺服器、點連線 | 點擊 |
| 5 | 確認畫面按「下載並安裝」 | 點擊 |
| 6 | 等待下載 | 等待 |
| 7 | 按「立即重新啟動」 | 點擊 |
| 8 | 若非 quickPlay 啟動，再點一次伺服器 | 點擊 |

**合計：3 次手動安裝 + 4 次點擊。** 離「一鍵」還很遠，而最大的阻力在第 1～3 步——那三步是這個模組**自己無法解決**的，因為它得先存在於玩家的遊戲裡才能運作。

---

## Phase 1：讓「第一次」變成一鍵

這是投報率最高的一個階段。做完之後第 1～3 步與第 5 步一起消失。

### 1.1 匯出 Modrinth 整合包（`.mrpack`）— ✅ 已完成

`/automodfetcher export` 產生 `.mrpack`，內含所有可解析的模組與 Fabric Loader 版本宣告。
AutoModFetcher 本身也會被包進去——它的 jar 就在伺服器的 mods 資料夾裡，上架平台後跟其他模組一樣
靠雜湊查得到。沒有它的話，整合包只是當天的快照，收不到之後的更新。

驗證方式是把包裡每個網址真的下載一次、比對雜湊與大小，而不是看 JSON 覺得長得對。
這揪出了一個 rebuild 項目的 SHA-1 不一致問題。

<details><summary>原始規劃</summary>

伺服器產生一個 `.mrpack`，內含 Fabric Loader 版本宣告、Fabric API、AutoModFetcher，以及所有可配送的伺服器模組。玩家用 Prism / Modrinth App / ATLauncher 匯入 —— **一次點擊，全部就位**。

可行性很高：我們早就有全部所需資料（檔名、SHA-512、大小、下載網址、環境標記）。`.mrpack` 本質上就是一個 zip 加一份 `modrinth.index.json`。

做完之後玩家的流程變成：匯入整合包 → 開遊戲 → 進伺服器。**1 次匯入 + 1 次點擊。**

AutoModFetcher 在這之後負責的是**後續更新**，而不是初次安裝——那才是它真正擅長的事。

</details>

### 1.2 匯出 CurseForge 整合包 — ✅ 已完成

`.mrpack` **不能**涵蓋所有人。實際支援情況：

| 啟動器 | `.mrpack` |
|---|---|
| Modrinth App / Prism / ATLauncher / GDLauncher | ✅ 原生支援 |
| CurseForge App | ❌ 只吃自己的 `manifest.json` 格式 |
| 官方 Minecraft 啟動器 | ❌ 不支援任何整合包格式 |

所以 1.1 和 1.2 是**兩種格式都要出**，不是先做一個再看看。

### 1.3 官方啟動器玩家：整合包幫不了他們 — 部分完成

官方啟動器沒有「整合包」這個概念——這不是支援得差，是功能不存在。而那正是最沒經驗、最需要「無腦」的那群玩家。

**對他們來說 AutoModFetcher 不是輔助，是唯一解。** 能壓縮到的極限是：

1. 跑一次 Fabric 官方安裝程式（一鍵）
2. 把一個 zip 解壓進 `mods/`（一次拖曳，內含 Fabric API + AutoModFetcher）
3. 之後全部自動

模組內已加上兩項協助：**「開啟 mods 資料夾」按鈕**（新手最常卡在找不到路徑），以及
**共用 `.minecraft` 的警告**（否則伺服器的模組會跟著出現在他所有的單人存檔裡）。

起始包 zip 尚未製作——那是文件素材而非產品的一部分，隨時可做。Fabric API 是 Apache-2.0，可合法轉載。

### 1.4 `/automodfetcher export` 指令 — ✅ 已完成

管理員在伺服器輸入指令就產生整合包檔案，不必手動整理。

---

## Phase 2：讓「之後每次」不再需要點

### ~~2.1 重啟後自動回到伺服器~~ — 已否決

曾經提案：重啟時附上 `--quickPlayMultiplayer <剛才那台伺服器>`，讓玩家按一次就回到遊戲裡。

**不做。** 那會讓「重新啟動」這顆按鈕做了兩件事，而玩家只要求了一件——他按下去是為了讓模組生效，不是為了被丟回某台伺服器。想去別台、或只是想先看看的玩家，會被莫名其妙連到舊的地方，甚至因為模組組合變了而被踢。

現行行為（原封不動沿用原始啟動參數）是對的：本來就用 quickPlay 捷徑開遊戲的人會回到同一台，那是他自己一開始選的，不算意外。**我們不主動加上原本沒有的東西。**

### 2.2 記住「這台伺服器的更新自動套用」 — ✅ 已完成

第一次連線仍然詢問，畫面上有一個**要玩家主動勾選**的選項——同意一次不等於永遠同意。
勾了之後記住這個決定，之後這台伺服器的更新直接下載，不再跳確認畫面。

**白名單與雜湊驗證照常執行**——我們拿掉的是重複的詢問，不是安全檢查。管理員每週換模組不該讓玩家每週被問一次。

信任只涵蓋「安裝」，不涵蓋「沉默」：有被拒絕或需手動安裝的項目時照樣跳畫面，因為那是要告訴玩家的事。

---

## Phase 3：讓它不會半路失敗

這些不減少點擊次數，但**一次失敗就毀掉整個「無腦」體驗**。

### 3.1 下載失敗自動重試 — ✅ 已完成
每個檔案 3 次嘗試，間隔 1 秒與 2 秒。被白名單拒絕的不重試——那是決定不是故障。

### 3.2 HTTP Range 續傳 — ✅ 已完成
重試時帶上 `Range` 標頭接續已下載的部分。摘要會先餵入磁碟上既有的位元組，所以雜湊驗證涵蓋整個檔案。
伺服器忽略 Range 回 200 時，既有片段會被丟棄重來——留著只會做出壞檔案。

### 3.3 進度畫面顯示速度與剩餘時間 — ✅ 已完成
指數平滑的速率，取樣間隔 400 毫秒。從工作階段開始算的平均值會在傳輸卡住後仍報出漂亮的數字，所以近期樣本的權重高得多。

---

## Phase 4：管理員這端的摩擦

玩家的體驗取決於管理員有沒有把伺服器設好。

### 4.1 `/automodfetcher reload` — ✅ 已完成
重建清單而非重載模組——**模組無法熱重載**。指令會比對資料夾與 Fabric 實際載入的內容，點名「已放入但沒在跑」的 jar。

### ~~4.2 伺服器列表預先檢查~~ — 已否決

曾經提案：在多人遊戲列表標示「需要 3 個模組」。

**不做。** 要實現得在伺服器狀態回應裡塞自訂欄位，兩端都得 mixin 才能讀寫。而代價換來的東西是零——玩家看到標示之後，還是點進去、看到畫面、按下載，動作數完全一樣。它只是把同樣的資訊提早幾秒顯示。

---

### 4.3 分發沒有平台收錄的模組：`/automodfetcher bundle` — ✅ 已完成

`manualUrls` 早就存在，但**它其實從來沒能用**：客戶端的 `allowedDomains` 只放行三個平台 CDN，
管理員填的任何自架網址都會在 `DownloadSession` 被擋掉，玩家只會看到一句「去改 client.json」。
那不是逃生門，是一扇畫在牆上的門。

現在：`/automodfetcher bundle` 把**兩個平台都查不到**的檔案打包成一個 zip，管理員上傳到任何地方，
網址填 `bundleUrl`。客戶端整包驗 SHA-512、解壓、逐檔再驗一次。
`/automodfetcher bundle verify` 會實際抓一次那個網址，抓出「分享頁不是直接下載」和「改了模組忘記
重新上傳」這兩種從管理員的位置看不出來、卻只會在玩家機器上爆掉的錯。

配套的是把白名單以外的來源從「靜默封鎖」改成「明確詢問」：確認畫面標示網站與檔案，
同意只對**該伺服器**生效，不寫回 `allowedDomains`。詳見 `.claude/README.md` 的安全模型第 5 道。

---

### 4.4 多餘的模組：加入即斷線的真正兇手 — ✅ 已完成

實測抓到的：朋友拿舊測試包的實例連新伺服器，AMF 把 28 個該裝的都裝好了，他仍然在加入後
**一秒**被踢。原因是他手上還有 17 個舊包留下、新伺服器沒有的模組（`aether`、`explorations`、
`moogs_structures`、`spell_power`……），registry 對不上。

AMF 當時對此完全是瞎的：`SyncPlanner` 只走訪 `manifest.entries()`，`PendingDiagnosis` 只帶
`manual` 與 `blocked`——整個系統只回答「你缺什麼」，沒有一處在看「你多了什麼」。而下載清單
永遠表達不出這件事。

現在 manifest 多帶一份 `serverModIds`，客戶端相減就知道自己多了什麼，列出來，經同意後搬到
`mods-disabled-by-automodfetcher/`。**搬而不刪**，純客戶端模組不點名。

同時修掉一個相鄰的坑：移除／搬走要重啟兩次才乾淨（loader 在任何模組跑起來之前就掃完
`mods/`，所以那一輪仍然載入著被移除的 jar）。`ClientSync` 現在會擋下這一輪的連線，
叫玩家再重開一次，而不是讓他撞上同一個看不懂的斷線。

---

## Phase 5：觸及率

### 5.1 支援 1.21.x — ✅ 1.21.1 已完成

核心邏輯（雜湊、解析、下載、驗證）與版本無關，46 個檔案裡只有 9 個需要動。用 Stonecutter
的條件註解處理差異，一份原始碼同時建出兩個版本。

事前假設錯了一半：以為要改寫成 configuration 階段，實際上 **login query 在 1.21.1 還在，
而且比 configuration 更早**，所以兩個版本共用同一條路徑。真正要改的是 GUI（`renderBackground`
多了三個參數、`mouseScrolled` 拆成水平與垂直、`Checkbox` 改成 builder）和一件本來沒預料到的事：

**1.20.2 把伺服器資訊從 login handler 拿掉了。** 1.20.1 可以直接讀 `serverInfo`；1.21.1 得改從
連線畫面拿 socket 位址，而且拿不到 `ServerData`——所以改用位址現組一個，跟「直接連線」產生的
是同一種東西。信任與略過決定都是拿位址當 key，這樣兩版行為一致。

驗證方式一樣是真的跑：真實 Fabric 1.21.1 伺服器 + 真實客戶端，確認畫面 → 下載 → 重啟 → 成功進入遊戲。

### 5.2 支援 Forge 與 NeoForge — ✅ 已完成

四個目標：**Fabric 1.20.1 / Fabric 1.21.1 / NeoForge 1.21.1 / Forge 1.20.1**，一份原始碼，
Stonecutter 一個節點一個組合。

要問 loader 的問題收斂成 `Loader` 一個介面——遊戲目錄、實際載入了哪些 jar、我們自己的 jar
是哪個、loader 叫什麼名字。其餘碰到 loader 的都是進入點，薄到只剩一行 `init()`。

**最大的意外是：Forge 和 NeoForge 都會在我們開口之前就把玩家踢掉。**

NeoForge 在 configuration 階段一開始做頻道協商，不合就斷線，任何 configuration task 都排在
它後面。Forge 更早也更嚴——缺一個模組就夠了，不必牽涉頻道。兩邊都只剩一個地方還來得及：
**login 階段**。Fabric API 直接提供 login query，另外兩個沒有，所以用 mixin 補上。

| | mixin 數 | 為什麼 |
|---|---|---|
| Fabric | 0 | Fabric API 本來就有 |
| NeoForge 1.21.1 | 4 | 兩個攔封包（vanilla 對不認識的 login payload 是讀完就丟），兩個負責問與答 |
| Forge 1.20.1 | 1 | 1.20.1 的封包直接帶 buffer，而且 Forge 把 tick 與封包都繞過 `NetworkHooks` |

一路上被版本差異告知的三件事：access transformer 在 Forge 1.20.1 是 SRG、在 NeoForge 是
Mojang 名字；mixin config 在 Forge 1.20.1 宣告在 jar manifest 而不是 mods.toml；Forge 和
NeoForge 都不保留啟動參數，所以「立即重新啟動」在那兩邊不提供，完成畫面退回「關閉遊戲」。

四個節點都是真伺服器 + 真客戶端跑過整段旅程，包含「沒裝這個模組的客戶端照樣進得去」。

### 5.3 更多語言
目前只有繁體中文與英文。

---

## 有一件事永遠無法一鍵

**作者關閉第三方下載的模組，我們永遠裝不了。**

`torch-arrow` 就是實例：CurseForge 認得那個檔案，但拒絕提供下載網址，因為作者這樣設定。我們能做的只有給玩家一個官方頁面連結，讓他自己去下載——那一步無論如何都是手動的。

這不是待辦事項，是設計約束。真正的解法在管理員端：**選用可配送的模組**。伺服器啟動時的警告訊息已經會點名這類模組，管理員應該把它當成選型依據。

---

## 明確不做

**內建 HTTP 檔案伺服器。** 它會讓管理員很容易在不知情的情況下轉載別人的模組。`torch-arrow` 的案例已經示範了這條線多容易踩過去——對一個要商業發布的模組來說，提供這種功能等於在幫使用者製造法律風險。

> 4.3 的 bundle **不是**這一項，這裡不是把約束偷偷放掉了。差別在三處：檔案由管理員自己上傳到自己的空間（模組不提供任何檔案服務）；能進包的只有**兩個平台都查不到**的檔案，`BundleBuilder` 硬性排除有 `pageUrl` 的（＝作者關閉了第三方下載）；而且那個排除會在指令輸出裡逐檔點名，管理員不可能不知情。這一項要否決的是「順手就能把整個 mods 資料夾轉載出去」，bundle 恰好做不到那件事。

**繞過作者的第三方下載設定。** 技術上做得到（拼 CDN 網址），但那是明確違反作者意願與平台條款。
